<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.homepage_v2.user.UserMapper">
<select id="userGet" parameterType="string">
	SELECT IADMIN, ID, PWD, ROLE FROM TBL_ADMIN WHERE ID = #{id}
</select>
</mapper>